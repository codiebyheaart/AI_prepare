spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/finsight
    username: finsight
    password: finsight123
  data:
    redis:
      host: redis
      port: 6379
  ai:
    vectorstore:
      pgvector:
        initialize-schema: true

app:
  cors:
    allowed-origins: http://localhost:3000,http://frontend:80
